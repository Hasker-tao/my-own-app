# project005 源码协作

在本仓库中工作时，先阅读 README 和相关功能文档；若仓库位于受治理的上层工作区，同时遵循该工作区的 AGENTS.md。

保留 Git 历史与本地 `MuziWorkspace` 业务数据。当前学习总文档仅 `docs/LEARNING_V1.md`；历史资料在分类 archive 目录，不作为当前需求。界面品牌使用小写 hasker，不盲目修改 MUZI_DATA_DIR 等兼容标识。上游 LICENSE 原样保留。

使用项目 Node 工具链；所有修改以用户要求为限，先明确假设与验证标准，采用最简单实现，仅修改必要内容并完成对应验证。
